"Subscribes to the changes of the given node."
subscription NodeSubscription($id: Int!) {
  node(nodeId: $id) {
    id
    contents {
      entries {
        molecule {
          name
        }
        concentration
      }
    }
  }
}
