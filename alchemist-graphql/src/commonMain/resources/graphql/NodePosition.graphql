"Returns the position of the given node."
query NodePosition($nodeId: Int!) {
    nodePosition(nodeId: $nodeId) {
        dimensions
        coordinates
        ... on Position2DSurrogate {
            x
            y
        }
    }
}
